create table account  (accountid varchar(10) not null, name
varchar(100) not null, address varchar(250) not null, city
varchar(30) not null, country varchar(30), zipcode varchar(15)
 not null, primary key(accountid)) in dbadmin2;

create unique index idx_account on airport(accountid);

